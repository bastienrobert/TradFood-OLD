<%= form_for(city) do |f| %>
  <% if city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, required: true %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description, required: true %>
  </div>

  <div class="field">
    <%= f.label :status %>
    <%= f.number_field :status, required: true %>
  </div>

  <!-- <div class="field">
    <%= f.label :status %>
    <%= f.select(:status) do %>
      <% [['Draft', 0], ['Publish', 1], ['Star', 2], ['Delete', 3]].each do |s| -%>
        <%= content_tag(:option, s.first, value: s.last) %>
      <% end %>
    <% end %>
  </div> -->

  <div class="field">
    <%= f.file_field :image %>
    <%= f.text_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
