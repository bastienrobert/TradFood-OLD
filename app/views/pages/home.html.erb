<section id="index">
  <div class="head">
    <h1>Découvrez<br>les meilleurs plats<br>traditionnels Bordelais</h1>
  </div>
  </div>
  <div class="search">

    <%= simple_form_for :search, html: {class: "search-bar"}, url: cities_search_path do |f| %>

      <div class="field for-type">
        <%= f.input :smcd, label: "Type", collection: {'Tout' => 'all','Entrée' => 'starter','Plat' => 'mainCourse','Dessert' => 'dessert'}, html: {id: "type", name: "type"}, selected: "Tout", required: false %>
      </div>

      <div class="field for-type">
        <%= f.input :difficulty, label: "Difficulté", collection: {'Tout' => 'all','Facile' => 1,'Moyen' => 2,'Difficile' => 3}, html: {id: "difficulty", name: "difficulty"}, selected: "Tout", required: false %>
      </div>

      <div class="field for-type">
        <%= f.input :season, label: "Saison", collection: {'Tout' => 'all','Ete' => 'summer','Automne' => 'automn','Printemps' => 'sping', 'Hiver' => 'winter'}, html: {id: "season", name: "season"}, selected: "Tout", required: false %>
      </div>

      <%= f.submit 'Rechercher', class: "button" %>

    <% end %>
  </div>
  <div class="concept">
    <h3><em>Le concept</em></h3>
    <div class="container">
      <div class="paragraph">
        <p>Trad’ Food est une plateforme en ligne ouverte à tous. <br>
        Elle vous permet, que vous soyez locaux ou touristes, de découvrir ou de redécouvrir, les recettes les plus emblématiques du terroir bordelais. </p>
        <p>Avec un panel de recettes triées par difficultés, saisons, et types de plats, le site vous propose de réaliser par vous même les recettes bordelaises qui font la richesse gastronomique de la région bordelaise.</p>
      </div>
    </div>
  </div>
  <div class="cases top-recipes">
    <h3><em>Les tendances</em></h3>
    <div class="container">
			<% Recipe.where(status:2).limit(3).each do |recipe| %>
				<%= link_to recipe do %>
					<div class="bloc" style="background-image:url(<%= recipe.image.url(:big) %>)">
						<div class="on-hover">
              <div class="infos">
                <ul>
    							<strong><li><%= recipe.name %></li></strong>
    						</ul>
              </div>
						</div>
					</div>
				<% end %>
			<% end %>
    </div>
  </div>
  <div class="cases random-recipes">
    <h3><em>Les recettes à l'affiche</em></h3>
    <div class="container">
			<% Recipe.limit(12).reverse.each do |recipe| %>
				<%= link_to recipe do %>
					<div class="bloc" style="background-image:url(<%= recipe.image.url(:medium) %>)">
						<div class="on-hover">
              <span><em><%= recipe.name %></em></span>
						</div>
					</div>
				<% end %>
			<% end %>
    </div>
  </div>
</section>
